Skip to content
Search or jump to…
Pull requests
Issues
Marketplace
Explore
 
@rehanqasimk 
adil-innovation-lab
/
react-expense-tracker
Public
Code
Issues
37
Pull requests
15
Actions
Projects
Wiki
Security
Insights
react-expense-tracker/.github/workflows/DeploySurge.yml
@adil-innovation-lab
adil-innovation-lab Create DeploySurge.yml
…
Latest commit a2030fa on Jun 15, 2020
 History
 1 contributor
41 lines (32 sloc)  1.19 KB


# This is a basic workflow to help you get started with Actions

name: React App Deploy to Surge

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2
    
    - name: Install NodeJS
      uses: actions/setup-node@v2-beta
      with:
        node-version: 12
    
    - uses: borales/actions-yarn@v2.0.0
      with:
        cmd: install # will run `yarn install` command
        
    - name: Build React App
      run: yarn build
      
    - name: Install Surge
      run: npm install --global surge

    - name: Deploy to Surge
      run: surge ./build todolist_app.surge.sh --token ${{ secrets.TO_DO_LIST }}
